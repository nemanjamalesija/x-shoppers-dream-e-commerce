import './slider.css';

const Slider = () => {
  return (
    <section className="section-slider">
      <div className="container container-slider"></div>
    </section>
  );
};

export default Slider;
