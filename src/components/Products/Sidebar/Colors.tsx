import React from 'react';
import { filters } from '../../../constants/types/productsTypes';

const Colors = ({ currentColor, colors }: filters) => {
  return <div>Colors</div>;
};

export default Colors;
